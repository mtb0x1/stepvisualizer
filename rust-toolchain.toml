# Nightly toolchain required for std::arch::wasm32::v128 SIMD intrinsic support.
[toolchain]
targets = ["wasm32-unknown-unknown"]
components = ["rustfmt", "clippy"]
features = ["simd"]
channel = "nightly"
